Fixes alarms firing hours after an event passed. Stuck countdowns now cancel at fire time (not event end). Orphan AlarmKit alarms are cleaned up when you reopen the app. Optional calendar color tint for Dynamic Island (Settings → Look).
